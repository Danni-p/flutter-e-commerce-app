class DPricingCalculator {
  DPricingCalculator._();

  // calculate Price based on Tax and shipping
  static double calculateTotalPrice(double productPrice, String location) {
    // double taxRate = getTaxRateForLocation(location);
    return 0;
  }
}